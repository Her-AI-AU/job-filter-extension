export default {
  testEnvironment: "node", // Set the test environment (node, jsdom, etc.)
  verbose: true, // Display individual test results
  testMatch: ["**/__tests__/**/*.js?(x)", "**/?(*.)+(spec|test).js?(x)"], // Patterns for finding test files
};
